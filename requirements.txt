# Core data
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
scipy>=1.12

# Retrieval
sentence-transformers>=3.0
faiss-cpu>=1.8
rank-bm25>=0.2.2

# torch is installed separately: the wheel depends on the CUDA version, and
# recent GPUs (Blackwell / sm_120) need a build newer than the default one on
# PyPI. Pick the right command from https://pytorch.org/get-started/locally/
# CPU-only works for everything here except encoding the full catalogue.
torch>=2.4